apiVersion: v1
kind: Pod
metadata:
  name: fastapi-ml
  labels:
    app: fastapi-ml
spec:
  containers:
    - image: fiztehno/online_inference:v2
      name: fastapi-ml
      ports:
        - containerPort: 8000
